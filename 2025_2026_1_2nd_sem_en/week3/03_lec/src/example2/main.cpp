#include <iomanip>
#include <iostream>

int main() {
    // double x = 0.1 + 0.2;
    // std::cout << std::fixed << std::setprecision(17) << x;

    // double a = 0.0;
    // int count = 0;
    // while (a != 1.0) {
    //     a += 0.1;
    //     std::cout << std::fixed << std::setprecision(20)<< a << '\n';
    //     if (count++ == 20) {
    //         break;
    //     }
    // }

    int a = 10;
    a += 11; // a = a + 11
    std::cout << a / 6.0 << '\n';
}